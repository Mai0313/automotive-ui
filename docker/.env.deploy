# OpenAI Configuration (OPTIONAL)
# These variables are optional. If not configured, AI-related features will be disabled with console warnings.
# To enable AI features (voice assistant, vehicle warning suggestions), configure the following:
EXPO_PUBLIC_OPENAI_BASE_URL=http://localhost:8889/v1
EXPO_PUBLIC_OPENAI_API_KEY=local
EXPO_PUBLIC_OPENAI_MODEL=meta-llama/Llama-3.1-8B-Instruct

# Backend Server Configuration (REQUIRED)
# WebSocket server URL for real-time communication
EXPO_PUBLIC_WS_SERVER_URL=ws://localhost:4000
# HTTP server URL for REST API fallback
EXPO_PUBLIC_HTTP_SERVER_URL=http://localhost:4001
# Realtime voice WebSocket server URL
EXPO_PUBLIC_REALTIME_VOICE_URL=ws://localhost:8100/ws
# Streaming TTS WebSocket server URL
EXPO_PUBLIC_STREAMING_TTS_URL=ws://localhost:8100/tts

# Database Configuration (REQUIRED)
# PostgreSQL connection URL for both server.js and scripts/init_db.tsx
# Note: init_db.tsx will create the 'automotive' database if it doesn't exist
# server.js connects to the 'automotive' database for real-time updates
POSTGRES_URL=postgresql://postgres:postgres@postgresql:5432

# dev
POSTGRES_PORT=5432
